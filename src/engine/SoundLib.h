#pragma once

void getWaveParameters(unsigned int wave, int note, unsigned char* page, unsigned int* fine, int* semi, unsigned char* wsr);
void getWaveParameters(unsigned int wave, int note, unsigned char *page, unsigned char *wsr);
